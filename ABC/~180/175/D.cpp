#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int64_t n, k;
    cin >> n >> k;
    
    vector<int> p(n);
    vector<int> c(n);
    for(int i = 0; i < n; i++){
        cin >> p[i];
    }
    for(int i = 0; i < n; i++){
        cin >> c[i];
    }
    vector<int> vec(n);
    for(int i = 0; i < k; i++){
        
    }
    
    
    
    
    
}
    