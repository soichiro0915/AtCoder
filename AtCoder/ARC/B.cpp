#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define _GLIBCXX_DEBUG

int main() {
    int n;
    string s;
    cin >> n >> s;
    
    rep(i, n){
        for(int j = i+1; j < n; j++){
            if(s[i] == 'A' || s[i] == 'T'){
                
            }
        }
    }
}